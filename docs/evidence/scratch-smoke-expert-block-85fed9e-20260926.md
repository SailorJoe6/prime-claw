# Independent EXPERT BLOCK — scratch smoke candidate `85fed9e`

- Exact reviewed pushed commit: `85fed9e281e91e89206a3eb18a21e845b382e70b` (`fix/episode-id-authority`).
- EXPERT: `scratch-smoke-expert-85fed9e`; session `01a0df53-9652-7389-8ce3-1d270fe30085`; child `sub-3f5b3816`; returned model `openai-codex/gpt-6-astra`; admitted thinking `max`.
- Validated profile: `.prime/agent/profiles/expert-reviewer.md`; one-shot packet SHA-256 `e540bab5ca4be20a78ecf9ad303f9d369d248f9dcf7fb5756bccf5a9dbe30f3c`, delivery `agentmsg_44570f08-e0d9-4fe7-a7c9-1c6faae3fbdd` (`delivered`).
- Verbatim review body SHA-256: `b7d1ca8bf40e1748e72ff2115a402a5f8142aca2907c77eb6f916a2ef1ef724b`.
- Disposition: **BLOCK**. Five material, actionable findings B1–B5. This is not a retryable review failure; do not run native preflight or `--run-smoke` on this candidate.
- Review stayed read-only and fake-only. No native Prime Agent CLI/daemon/socket, live EPISODE action, core/plugin edit, handoff, merge, or cleanup occurred.
- Owner classification: B1–B5 are accepted for an **in-scope revise** of the approved first default-socket/no-prompt teardown-smoke driver. The separate faux-provider/tool-root experiment and live EPISODE remain outside this revision. The wrapper-only PASS stays separate. A materially repaired candidate needs a fresh exact-commit EXPERT review.

## Verbatim reviewer report

BLOCK — exact candidate 85fed9e281e91e89206a3eb18a21e845b382e70b. Five material findings remain. Do not run native preflight or --run-smoke yet.

IDENTITY AND REVIEW EVIDENCE
- Worktree /Users/jlanders/code/prime-claw-episode-id-authority: HEAD and local origin/fix/episode-id-authority both equal the exact candidate. Branch is fix/episode-id-authority. Tracked/untracked status and staged/unstaged diffs were clean before and after review; commit diff --check passed. No fetch/network was used.
- Public source clone is clean at e260085dd8f742e0def3d871860c9a888b114851. Source citations below use packages/coding-agent/src/ relative to that clone, not the installed binary.
- Actual reviewer: openai-codex/gpt-6-astra, thinking max. Own session 01a0df53-9652-7389-8ce3-1d270fe30085 records model_change 13fca7bd and thinking_level_change d3eca5f8. Independent bounded EXPERT review only.
- Independently ran the checked-in driver suite: 17/17 PASS (python3 -B -m unittest discover -s tests -p test_prime_agent_scratch_smoke.py -v). Confirmed first that every CLI/lsof/ps invocation is injected or mocked. Also ran 10 in-memory Python-only review cases: eight failed invariant assertions, one unhandled injected exception, one passing preliminary permission-injection check. The actual unreadable-subtree case used only a test-owned directory, restored its permissions, and confirmed the failure below. No repository test/source file was changed.
- No installed Prime Agent CLI, native preflight, smoke, daemon, socket, provider/network, credential store, live EPISODE action, commit, cleanup of external roots, or lifecycle mutation occurred. Only Python-owned test fixtures were created and removed. No report file was written because review is read-only.

WHAT IS ALREADY SOUND
Root/co-root uid/mode, environment, PATH and socket-byte guards are explicit. Recognized failed baselines admit no mutation; recognized competing starts revoke shutdown. There is one start and one top-level shutdown call site, no retries/signals/root deletion. Failed shutdown is not promoted by empty scans. Normal command output is digest-only and authenticationToken is omitted. Native JSON/start shapes match cli/daemon-ps.ts:53-75,361-427,533-680 and cli/daemon-command.ts:680-718. These positives do not resolve the findings below.

B1 — P1 / HIGH, confidence 10/10: ownership evidence can be discarded or ignored before forced shutdown.
Evidence: driver :264-269 replaces any partially collected observation with a fresh invalid Observation. At :384-403 ownership checks run only when active.valid, except for a string search for "out-of-root". Therefore a daemon-ps result already showing a foreign PID, followed by lsof failure, loses that foreign evidence and reaches shutdown at :412. My fake case expected one command but observed two. Separately, :305-306 stores registry filenames without parsing owners; :384-403 never reconciles them. A native-shaped owner.json naming PID 456 against launched/current PID 123 likewise admitted the second command in a fake test. parse_daemon_ps :151-164 also discards sessionCount and hasTrackedWorkers supplied by v0.9.6, so the active decision cannot enforce the no-session/no-worker claim from that evidence.
Impact/invariant: contract :10-11 requires a newly observed foreign/worker identity to revoke authority. A scoped --force shutdown is still destructive to a competing daemon within that namespace; native cli/daemon-ps.ts:698-805 can terminate hidden/residual listeners. Later returning unresolved does not undo that mutation.
Root cause: scan success is incorrectly coupled to retention of ownership facts, and registry ownership is treated as a filename inventory rather than independent authority evidence. Error-message substrings are being used as lifecycle state.
Repair direction/rationale: make adverse ownership evidence monotonic across partial scan failures. Reconcile the expected owner with redacted native registry identity/scope and the daemon-ps session/worker indicators before shutdown admission. Use explicit typed uncertainty/revocation reasons. If a failure prevents safe reconciliation, retain/quarantine rather than infer ownership. This preserves the existing uncertain-start policy without allowing known competing evidence to disappear.
Constraints/avoid: do not simply suppress registry files, trust a daemon status of current as exclusive ownership, or infer safety from an isolated path alone. Do not replace this with global/per-socket shutdown, signals, retries, or a worker/session experiment.
Tests: positive current supervisor plus matching registry, zero sessions and no workers permits exactly one shutdown. Negative foreign PID, registry PID/start/socket mismatch, extra owner, sessionCount>0 or hasTrackedWorkers must forbid it. Failure tests must inject a later lsof/ps/filesystem error after each adverse fact and verify revocation survives. Replay the same retained root and verify zero further mutations.
Regression/dependencies: avoid misclassifying the normal owner record as foreign; preserve bounded handling of uncertain starts with no competing evidence. Repair with B2/B3 because their observation representation supplies the authority decision.

B2 — P1 / HIGH, confidence 10/10: incomplete or invalid observations can be accepted as empty/quiet.
Evidence: parse_lsof :120-138 silently ignores unrecognized records. An exit-0 injected "garbled-output" scan became a valid empty baseline. _observe :278 uses Path.rglob, which suppresses directory traversal errors. A test-owned unreadable subtree containing a file, after injected successful OS scans, also became a valid empty baseline. The installed system Python 3.9 pathlib.py:592-608 explicitly suppresses PermissionError; the fake tests on Python 3.14 confirmed the same outcome with actual fixture permissions. Descriptor validation :285-304 accepts a small field subset that is not even a native tracked descriptor: a fixture missing workerId and recoveryJournalPath was accepted and snapshot_quiet returned true when its PID was absent. Compare native cli/daemon-ps.ts:1075-1088 and modes/daemon/daemon-worker-protocol.ts:146-174.
Impact/invariant: contract :9 forbids start AND shutdown on a failed/ambiguous baseline, and :12 requires complete scans and makes malformed/unknown descriptors unresolved. These false-negative observations can admit an unowned start or support a false stopped classification.
Root cause: "no recognized entries" is treated as "a complete successful scan," and descriptor validity is weaker than the native format being audited.
Repair direction/rationale: explicitly track successful completion of every scan. Use filesystem traversal that propagates errors, validate the supported lsof record grammar/completeness, and validate/normalize the relevant native descriptor formats before allowing stale retained metadata. Bound enumeration and metadata; inability to complete is uncertainty, not emptiness.
Constraints/avoid: do not use missing entries, ignored exceptions, returncode alone, or process-title filtering as absence evidence. Do not loosen root/symlink guards to accommodate fixtures. Retain valid stale files only with complete absence evidence; do not log descriptor tokens or arbitrary raw fields while diagnosing invalid records.
Tests: positive representative macOS lsof records (including legitimate unrelated/unnamed Unix entries), complete empty directory traversal and valid stale descriptors. Negative malformed/truncated lsof records and missing/unknown descriptor fields. Failure tests for unreadable directories, per-entry stat/read errors, symlinks, disappearance during traversal and output/enumeration limits. Each baseline failure must produce zero mutations; each postflight failure must prevent stopped; replay must stay nonmutating.
Regression/dependencies: parser tightening must not reject normal macOS non-root socket records by accident, and stale native artifacts must not be confused with active owners. Repair together with B1 and B3; update the fake OS fixtures, not only high-level Observation fixtures.

B3 — P1 / HIGH, confidence 10/10: the two quiet observations do not include the captured process identities.
Evidence: NativeRunner only calls ps for PIDs rediscovered in that particular observation (:311-319). An empty baseline/postflight runs no independent /bin/ps command at all; a fake assertion confirmed this. Captured PIDs are checked once, AFTER both postflights and the quiet interval (:420-441), not in each snapshot. A staged fake process still present at the first postflight but gone by the second was classified stopped because the only captured-PID check occurred last.
Impact/invariant: contract :9 and :12 require successful independent process checks and two complete quiet observations, including captured PID/start identities and possible replacements. The current report can claim a quiet interval that was never established for the captured process. An unavailable ps path also remains invisible when the rediscovered PID set is empty.
Root cause: process identity checks are a final add-on rather than part of the definition of each observation; empty candidate discovery is assumed to prove an empty process set.
Repair direction/rationale: make each postflight independently check the union of captured identities and newly discovered candidates, and record the success/completeness of the independent process observation even when no candidate is found. Reconcile replacements without treating PID disappearance in only the last check as two quiet snapshots.
Constraints/avoid: do not drop old PIDs just because socket/descriptor discovery no longer names them; do not weaken the existing PID-reuse veto, trust ps titles alone, or introduce signals/retries to force a quiet result.
Tests: positive two observations with all captured identities absent; negative captured process alive in the first or second, changed start identity, newly appearing PID/listener between scans. Failure tests for ps failure with empty discovery, malformed identity, or exception in either snapshot must remain unresolved. Same-root replay must make no new start/shutdown attempt.
Regression/dependencies: avoid counting the ephemeral observer commands themselves as retained daemon identities; keep observations bounded and redacted. Shares the observation model with B1/B2 and should be repaired with them.

B4 — P2 / MEDIUM, confidence 10/10: executable identity is checked only through a mutable pathname, and the retained record omits its path/command.
Evidence: validate_root :107-113 rejects only a symlink at the final filename and hashes the target before the experiment. A CLI path through a symlinked parent passed my fake guard test despite the documented "absolute resolved" requirement. NativeRunner.command :228-234 later checks only pathname membership, with no identity revalidation. report :333-335 records the supplied hash but not the executable path or exact argv/cwd promised by contract :10.
Impact/invariant: the approved experiment must use and record one exact executable for ps/start/shutdown. A parent alias/release retarget or replacement can select different bytes after the hash check, while the report still attributes them to the original SHA. Even without a replacement, the retained report cannot establish which physical executable and invocation were used.
Root cause: path spelling is conflated with physical executable identity, and invocation evidence is incomplete.
Repair direction/rationale: require and retain the canonical physical executable identity; verify it remains the admitted identity before native use, especially mutations, and record safe exact argv/cwd plus the identity. On identity change after start, retain an unresolved root instead of selecting another executable for teardown. This needs no installed-CLI invocation in the fake tests.
Constraints/avoid: no PATH fallback, silent alias resolution to a new generation, copying credentials, or substitution of a different CLI for shutdown. Keep the report bounded; do not dump the environment. The start --cwd already targets the scratch project; distinguish that from the helper process's inherited cwd when recording evidence.
Tests: positive canonical immutable fake executable and complete invocation record; negative final and parent symlinks, hash mismatch and missing executable. Failure tests replace/retarget the fake path between phases and ensure no changed executable is invoked. Replay an identity-failed root with zero new mutations.
Regression/dependencies: installed release-path conventions must still work when explicitly resolved. Coordinate with B5 so an identity-change refusal is recorded without bypassing the one-attempt rule.

B5 — P2 / MEDIUM, confidence 10/10: the finalizer is not exception-safe, and its claimed output bounds are post-hoc.
Evidence: the outer except ends at :405. The finally body performs stop.record, parse_shutdown, observation.record and record_report without an enclosing fail-closed evidence boundary (:406-445). Only UnsafeScratch is caught around parse_shutdown. An injected unexpected ValueError there escaped smoke, skipped both postflights and left no report; main :469-471 would emit only a generic blocked_unexpected response. Independently, :232-234 capture_output buffers stdout/stderr without a size limit; MAX_OUTPUT is checked only after completion in selected parsers, and the start output is not checked before hashing. Observation.record :65-66 also serializes unbounded collected fields/lists.
Impact/invariant: contract :12-13 requires retained bounded stage/classification evidence and independent postflight on failure. Unexpected finalizer errors lose that evidence; resource exhaustion can prevent the very teardown/report path intended to contain the experiment.
Root cause: cleanup and reporting are assumed reliable after entering finally; bounds are validation checks after allocation rather than limits on collection/retention.
Repair direction/rationale: treat shutdown attempt, result parsing, both observations and durable redacted reporting as separately guarded stages. Preserve a non-success classification if any stage fails; continue permitted read-only observations where possible without repeating mutation. Enforce actual collection/record budgets and retain safe failure categories/hash/count metadata. If writing the report itself fails, surface a bounded explicit evidence-write failure with the known retained root and manual-quarantine instruction.
Constraints/avoid: never turn a caught exception into stopped, retry shutdown, print raw parser/output exceptions containing secrets, or delete the root. Do not require a successful parser to reach the postflight/report logic.
Tests: positive normal report with exact one-start/one-stop counts. Negative malformed/deep/oversized stdout and oversized stderr/metadata. Inject exceptions at shutdown result recording/parsing, each postflight, PID checks, serialization and report writing; verify no second mutation, no secret leakage, and explicit unresolved evidence. Replay every interrupted stage against the same root without deleting report/project sentinels, and verify zero new mutation.
Regression/dependencies: preserve the distinction between failed native shutdown and failed observation/reporting, and keep useful diagnostics without raw tokens. Repair with B4 for identity evidence and B1 for authority-revoked paths.

TEST AND DOCUMENT RECONCILIATION
Many existing tests consume prebuilt Observations. Removing report/project sentinels between subcases (:125-134,150-183) is not replay coverage. Add real same-root nonmutation replay, custom-socket rejection, owner/mode/co-root failures, malformed successful starts, both quiet-scan failure positions, and representative native registry/descriptor fixtures. Reconcile candidate evidence after repaired fake tests and fresh exact-commit review.

No product decision or scope expansion is required. Keep the approved default-socket/no-session/no-prompt slice. This BLOCK does not question the separate wrapper-only PASS and does not authorize the faux-provider/tool-root experiment, a native run, live EPISODE work, merge, or cleanup.
