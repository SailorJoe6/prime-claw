BLOCK — exact pushed commit bafd0e3707eee55b9c129302c57166d9dea24d73. Three material findings remain. Do not run installed-CLI preflight or --run-smoke on this candidate.

IDENTITY / REVIEW EVIDENCE
- Reviewed only /Users/jlanders/code/prime-claw-episode-id-authority on fix/episode-id-authority. HEAD = local origin/fix/episode-id-authority = exact candidate; parent 60186e2036eac88157f217745f5a801f21078fa0. Clean tracked/untracked state at admission and final check; diff --check passed. Only the three packet-approved paths changed. No fetch/pull/network.
- Pinned public source clone is clean at e260085dd8f742e0def3d871860c9a888b114851. Native citations below are relative to packages/coding-agent/src in that clone. Prior R1–R4 report hash verified: aea151cbdf3fc091697002bd6889fb3b668bcf67636bf2f64aca2fd9fc2a1a42.
- Actual reviewer model/thinking: openai-codex/gpt-6-astra, max. Own session 01a0dfb0-ea1d-777c-92b6-c7e4cfe558ae records model_change 48dda7af and thinking_level_change de966108.
- Independently ran python3 -B -m unittest discover -s tests -p test_prime_agent_scratch_smoke.py -v: 45/45 PASS. Also ran the Python-owned wrapper suite: 7/7 PASS. Inspected fail-fast Popen/run/check_output guards before execution; explicit capture fakes use Python pipes, not child processes.
- Ran additional in-memory project-native python3 -B -c cases with outer subprocess guards, injected CLI/OS results, and test-owned temporary files. These exercised actual filesystem parsers plus full smoke classification, positive native-shaped controls, failure injection, bounded direct-child capture, redaction, and genuine same-root replay without deleting report/project sentinels. No report file was written to the repository. No installed Prime Agent command, daemon/socket/provider, credential store, Bead, core/plugin/global edit, live EPISODE action, merge, or external-root cleanup occurred.

WHAT IS NOW SOUND / PRIOR-FINDING DISPOSITION
The normal supervisor-config and snapshot-cache startup tree is recognized. Matching owner/scope, session/worker flags, partial scan failures, lsof grammar checks, and bounded filesystem traversal retain the earlier improvements. Captured active PIDs are checked in both postflights. Physical executable path/device/inode/hash checks and safe invocation records remain present; the declared macOS Python 3.9 non-atomic check-to-exec residual is not a new blocker under this bounded contract.
R2's quiet/PID-classification exceptions now retain an unresolved report after both observations. Report serialization/fsync failures do not return stopped; a file containing stopped after failed fsync is uncommitted evidence, not a successful returned verdict. R4's immediate post-Popen containment and finite wait arguments pass the checked-in failure injections. However, their result can still lose authority-critical facts at the next seam (C1). B2/B3 and R1/R3 remain incomplete at the native metadata boundaries (C2/C3). Wrapper-only PASS is still separate.

C1 — P1 / HIGH, confidence 10/10: command-result recording failure erases already-known shutdown revocation.
Evidence: driver scripts/validate-prime-agent-scratch-smoke.py:883-890 puts runner.command and outcome.record in one try. If recording fails, it replaces the outcome with CommandResult(None, timed_out=True), dropping child_unresolved and the already-running response. The downstream veto at :918-920/:929-932 therefore cannot see those facts.
I injected a start result with child_unresolved=True and a failing record(): the full smoke made TWO mutations, not one, and persisted unresolved rather than unresolved_ownership. An already-running start plus the same recording failure also admitted shutdown after an injected empty active scan. Both refused same-root replay, but the prohibited second mutation had already happened.
This is not limited to an exotic result subclass: I passed an exact-handle Popen fake through NativeRunner.command. It produced 400,000 invalid UTF-8 stderr bytes, could not be killed/reaped, and returned child_unresolved=True with finite waits. Decode replacement at :695-699 produced 1,200,000 encoded bytes; record() rejected that at :49-51. The wrapper discarded the flag and called shutdown. The retained start event contained only the generic recording error. All subprocess activity in this test was injected; no real child or daemon was launched.
Invariant/impact: known competing start evidence and an unconfirmed direct start child must revoke further mutation. A later empty scan cannot erase that fact. Otherwise shutdown can overlap a still-running start CLI, which may admit a detached daemon after the shutdown snapshot. Returning unresolved afterward does not undo that unauthorized lifecycle transition.
Root cause: evidence formatting is allowed to replace the control-plane result, so adverse facts are not monotonic across the command-to-record seam. This reopens B1/B5 and prevents complete R4 closure.
Repair direction/rationale: preserve independently captured lifecycle/revocation facts before attempting fallible recording. Recording failure should add uncertainty, not reset authority. Keep raw capture budgets and decoded evidence budgets consistent, or retain bounded raw-byte digests/counts without losing the status flags.
Constraints/avoid: do not infer child exit from kill(), clear already-running evidence after an empty scan, retry a mutation, substitute a CLI, signal arbitrary PIDs/groups, or print raw output. A conservative no-shutdown decision is preferable when result-state integrity itself is uncertain.
Tests: positive normal result and a reaped ordinary uncertain start preserve existing one-stop behavior; an unreaped result or already-running response without recording failure still produces one start only. Negative/failure cases combine each adverse fact with record(), encoding, digest/count, and size-limit failures; include the real NativeRunner fake-pipe path above. Require zero additional mutation, retained redacted revocation, and finite waits. Replay each retained failure root with zero new start/stop calls.
Regression/dependencies: preserve permitted uncertain-start teardown when no adverse fact exists; do not turn every capture problem into a false competing-owner claim. Repair this together with command evidence/report containment, not merely the NativeRunner flag producer.

C2 — P1 / HIGH, confidence 10/10: postflight validates metadata but drops discoverable process/generation identities before the quiet decision.
Evidence: validate_worker_journal at :501-526 returns no process facts. _observe_into calls it at :789-795, then builds the ps set from daemon rows, lsof refs, worker descriptor PIDs, registry owners and required_pids only (:800-804). An active orphan journal's pid/processStartId is never independently checked or retained. Native core/orphan-process-journal.ts:8-24,53-100 explicitly models these as active child identities; cli/daemon-ps.ts:971-1005 treats them as distinct from the worker PID.
My full-smoke fake placed a valid stale w1 descriptor (worker PID 234) and a native-shaped w1.orphans.jsonl active record for PID 345 with a start ID. Both postflights used the real filesystem scanner, with native ps/lsof discovery injected empty and captured supervisor/worker PIDs absent. The OS fake was configured to report PID 345 alive if asked. Neither snapshot asked: only observer, 123 and 234 were checked. Both snapshots were valid and the returned/persisted status was stopped. The same-root replay made no new mutation. This is an independent-proof failure under potentially degraded daemon discovery, not evidence that a real native process survived a run; no native run occurred.
Related generation seam: :748-753 collects any syntactically accepted snapshot generation, but :529-539 ignores it after active ownership was proven. A postflight-only snapshot-cache/foreign-generation, different from the captured g1 owner, also returned stopped. Native daemon-supervisor.ts:841,870-871 scopes that directory to the supervisor generation and :7493/:7577 removes that generation during cleanup. Its appearance is observable unknown/replacement state, not an undiscoverable future actor covered by the stated residual.
Invariant/impact: the contract requires two complete independent checks of captured and replacement process identities, and unknown/foreign state must remain adverse. A dead worker PID is not proof its recorded child is gone. An observed new supervisor generation cannot silently become ordinary retained metadata. These gaps can support stopped without the promised independent evidence.
Root cause: metadata shape validation, identity collection, and lifecycle reconciliation are separate lists with missing links. Known child identities are thrown away; collected generation evidence is not reconciled in the final snapshots.
Repair direction/rationale: carry redacted native child identity candidates and supervisor-generation provenance into the observation/reconciliation model. Include all applicable known and newly discovered PID/start candidates in each required independent check; preserve adverse or missing-identity facts across later disappearance/failures. Distinguish a captured generation's allowed retained residue from an unrecognized/new generation.
Constraints/avoid: do not rely on daemon ps to compensate for its documented best-effort descriptor discovery, assume no lsof root reference means a journaled child exited, parse only a process title, or kill the journaled PID. Keep both postflights read-only, bounded and token-free. Respect native latest-record/inactive semantics rather than blindly treating all historical journal rows as live.
Tests: positive source-shaped journal with all applicable identities absent in BOTH scans, valid inactive/latest-record behavior, and captured-generation residue allowed by the contract. Negative active orphan alive in either scan, PID reuse, active identity missing, new/changed generation, or an unreconciled candidate first found during postflight must prevent stopped. Failure tests inject journal read/stat/parse/ps failures after partial evidence and disappearance between snapshots. Require actual per-snapshot PID-call assertions, redacted retained facts, and nonmutating same-root replay.
Regression/dependencies: support valid stale native descriptors without converting malformed/unknown state into emptiness; avoid false blockers from historical inactive rows. Repair with C3's source-derived normalization and preserve B1's active worker/foreign-owner veto.

C3 — P2 / MEDIUM, confidence 10/10: native metadata validation still accepts malformed records and rejects a source-generated valid journal shape.
Evidence A: parse_worker_descriptor :415-422 validates only three optional paths inside legacy createCommand.config. It ignores the types of the remaining supported native fields. A v1 descriptor with config={telemetryDisabled:7, noTools:"invalid"}, otherwise valid native fields and absent identities, was accepted by the real filesystem scanner in both postflights and the full smoke returned/persisted stopped. Positive controls with valid v1 config and v2 durable createCommand also passed. Compare native core/agent-session-config.ts:6-45 and modes/daemon/daemon-worker-protocol.ts:177-211. Unknown top-level worker fields are also silently dropped; a fixture containing an extra routing object was accepted.
Evidence B: validate_command_journal :493-495 accepts response={type:"response"} without required command/success/error discriminants. A source-shaped received record plus result with a correctly paired native-format key but that incomplete response produced stopped in both full-smoke postflights. Native modes/daemon/daemon-protocol.ts:1026-1035 defines the required response union; command-recovery-journal.ts:16-22 records that type.
Evidence C: the same parser calls json.loads on every split line (:477-482). Native command-recovery-journal.ts:112-126,187-205 compacts an empty journal to a single newline; :139-142 explicitly accepts empty lines. A retained command-journal.jsonl containing exactly that newline was classified malformed in both driver postflights, causing unresolved despite otherwise valid empty observations and successful shutdown. This is a source-generated shape the candidate cannot validate, not a claim that every no-session shutdown necessarily reaches that compaction path. The checked-in tests use a standalone acknowledged record instead of exercising the compacted empty representation.
Invariant/impact: unsupported/malformed metadata must remain unresolved; supported normal no-session supervisor artifacts must not be rejected merely because the fake fixtures omit their real representation. Here invalid legacy/journal evidence can satisfy stopped, while legitimate journal residue cannot. R1/R3 are therefore not fully closed.
Root cause: hand-selected field checks and fixtures are not a version-aware normalization of the pinned native schemas and writer outputs. Classification treats successful partial validation as complete validity.
Repair direction/rationale: define the bounded supported v1/v2 and journal representations from native writers/types, normalize relevant legacy settings, validate known fields and discriminants, and distinguish legitimate blank/empty journal representation from malformed nonempty records. Keep explicit treatment of unsupported fields rather than silently deleting evidence.
Constraints/avoid: do not copy the native best-effort reader's permissiveness, waive arbitrary malformed lines, reject every legitimate legacy config, accept every filename/JSON object, or expose authenticationToken, config contents, or response data in diagnostics. This needs fake/source work, not a native experiment or new scope.
Tests: positive v1/v2 descriptors across real lifecycle/create shapes; optional config fields with correct types/scopes; received/result/ack records and the writer's compacted newline-only journal. Negative invalid known legacy fields, unsupported routing fields, missing/wrong response discriminants, malformed nonempty/truncated records and out-of-scope paths must block quiet/stopped. Inject traversal/read/serialization failures after valid earlier facts; require retained unresolved evidence and redaction. Replay all retained positive and rejected roots without clearing sentinels and assert no new mutation.
Regression/dependencies: tightening must not recreate R1 by rejecting supported native artifacts or silently bless arbitrary blank/corrupt data. Repair with C2 because normalized journal/descriptor data feeds the identity proof; keep the existing active worker veto and byte/entry bounds.

No product or scope decision is required. Keep the approved default-socket/no-session/no-prompt slice. Repair these lifecycle/metadata seams and obtain a fresh exact-pushed-commit review. This BLOCK does not authorize native use, the faux-provider/tool-root experiment, active Phase 3a EPISODE work, handoff, merge, or cleanup.
