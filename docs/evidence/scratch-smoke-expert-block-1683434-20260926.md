BLOCK — exact pushed candidate 168343436b05f9c584eb701f9dfc57a573302c05. Four material findings remain. Do not run installed-CLI preflight or --run-smoke on this candidate.

IDENTITY / REVIEW EVIDENCE
- Worktree /Users/jlanders/code/prime-claw-episode-id-authority; branch fix/episode-id-authority. HEAD and local origin/fix/episode-id-authority equal the candidate; parent is 50754e1f41db3d5d2438342377ad9fba854ef008. Tracked/untracked status was clean at admission and after review checks. Commit diff --check passed. Only the three allowed paths changed. No fetch or network occurred.
- Public source clone /tmp/prime-agent-096-audit-x0mq9_0a/source is clean at e260085dd8f742e0def3d871860c9a888b114851. Native citations below are relative to packages/coding-agent/src in that clone. Compared registry owner/scope, descriptor layout, ps identities and shutdown behavior read-only.
- Actual reviewer model: openai-codex/gpt-6-astra; thinking max. Own session 01a0df8a-3c5b-7071-a6d8-bfb36b26609d records model_change 24e65ec2 and thinking_level_change 25ce1412.
- Independently ran python3 -B -m unittest discover -s tests -p test_prime_agent_scratch_smoke.py -v: 37/37 PASS. Checked guards first: tests :85-88 patch Popen/run/check_output before runner use; explicit Popen replacements create only Python pipes. Imports do not launch commands. The guard prevents the previously described fake-shell escape. It proves non-execution, not necessarily test failure for every unmocked call: smoke.command catches general Exception. No installed CLI was invoked. I inspected but did not rerun the unchanged wrapper suite; the owner's 7/7 result remains separate evidence.
- Ran nine additional Python-only scenarios through project-native python3 -B -c, with an outer subprocess guard as well as fixture guards. These covered late classification/report errors, normal native supervisor metadata, malformed descriptor acceptance, child-capture construction/reaping, a positive native-shaped stale descriptor, and genuine same-root replay. Only owned temporary Python fixtures were created/removed. No repository file or durable review artifact was written. No Bead, core/plugin, daemon/socket, credential store, live EPISODE, merge, or lifecycle state was changed.

WHAT IMPROVED / PRIOR B1–B5 DISPOSITION
B1: incomplete active observations now revoke shutdown; partial daemon evidence is retained. Matching owner/scope, PID/start, socket-hash directory, zero sessions and no tracked workers are reconciled. B3: native observations include ps health and captured PIDs in both snapshots; persistence/reuse remains a veto. B4: canonical path, device/inode/hash, boundary checks and safe argv/cwd records are present. B2 and B5 are not closed, as demonstrated below. Output capture is now capped while reading, and normal output/token redaction and same-root nonmutation replay passed.

R1 — P1 / HIGH, confidence 10/10: the scanner rejects metadata that every normal no-session supervisor creates, so an owned native start cannot reach teardown.
Evidence: driver scripts/validate-prime-agent-scratch-smoke.py :575-587 recognizes only *.recovery.jsonl, *.orphans.jsonl and worker *.json files under the socket-hash directory. Every other regular file raises "unknown worker descriptor artifact". Native modes/daemon/daemon-supervisor.ts :257 names its file "supervisor-config"; :835-836 chooses that same directory; :867-869 always writes it before listening; :1733-1739 shows its native shape. It is not a worker descriptor. A full smoke fake that creates this native-shaped file after a good start returned unresolved_ownership, active.reason="unknown worker descriptor artifact", and only ONE command (start, no shutdown). Same-root replay added zero commands. The 37 tests' successful active fixture omits this normal native artifact.
Invariant/impact: the first slice must admit one scoped teardown of a successfully owned no-session start. Here the driver's own incomplete model deterministically revokes authority after launch, leaving the otherwise normal scratch daemon running and quarantined. This is not a foreign-owner case that can be resolved by operator approval to ignore uncertainty.
Root cause: the native descriptor directory is modeled as exclusively worker metadata, although it also contains supervisor state. This is the filesystem-classification to ownership-admission seam.
Repair direction: derive a bounded, explicit artifact model for the pinned no-session supervisor lifecycle. Distinguish and validate normal supervisor metadata from worker descriptors, with socket/agent scope reconciliation. This permits normal teardown without discarding unknown evidence.
Constraints/avoid: do not ignore the whole directory, allow arbitrary filenames/JSON, treat workers as ordinary supervisor files, loosen B1, or delete files to pass scans. No native experiment is needed for this repair.
Tests: positive native-shaped complete active tree, including supervisor-config and expected directories, permits exactly one stop; valid retained native artifacts permit two quiet postflights. Negative malformed/wrong-scope supervisor state, unknown artifacts and worker records still veto. Inject stat/read/traversal failures after ownership evidence. Replay every retained root without removing report/project sentinels and assert zero new mutation.
Regression/dependencies: repair with R3's format classification and retain B1's monotonic uncertainty. Inventory native artifacts throughout start/stop, rather than adding one broad filename exception; otherwise the next normal artifact can strand another daemon.

R2 — P2 / MEDIUM, confidence 10/10: exceptions during final quiet classification still bypass the retained report.
Evidence: observation() guards collection and record creation at driver :660-679, but snapshot_quiet and captured-PID reconciliation at :764-770 are outside an exception boundary. record_report is reached only afterward at :772. A JSON-serializable injected Observation(daemons=[1]) in the first postflight caused AttributeError in snapshot_quiet :396, after one start, one stop, and both postflights. No scratch-smoke-report.json existed. Injecting ValueError directly at snapshot_quiet produced the same loss. Same-root replay remained nonmutating because project already existed; it did not recover the missing evidence. main :796-798 offers only generic blocked_unexpected output.
Invariant/impact: prior B5 and contract require retained stage/classification evidence and fail-closed containment of unexpected finalizer errors. The experiment can finish mutation and lose its entire baseline/start/shutdown/postflight record. This case did not return stopped; the false contract-level stopped case is R3.
Root cause: collection is guarded, but downstream interpretation of a collected Observation is still trusted. There is no final evidence boundary covering classification.
Repair direction: treat quiet/PID reconciliation as fallible stages and ensure any failure yields a non-success classification plus bounded retained evidence. Keep a last-resort redacted reporting path; if evidence writing itself is impossible, give the explicit root/quarantine failure required by the contract.
Constraints/avoid: no retry, extra shutdown, root removal, raw exception text, or converting an exception into an empty observation. Preserve already captured adverse facts.
Tests: positive normal classification still stops once. Negative malformed-but-serializable fields cannot report stopped. Inject failures in each quiet check, PID reconciliation, report serialization and file/directory fsync. Require the retained unresolved record, or an explicit bounded evidence-write failure when persistence itself fails, with no second mutation. Replay each failed stage without deleting sentinels.
Regression/dependencies: retain both permitted read-only postflights even when classification fails; keep output redacted and respect ownership revocation. Repair with R4 as the remaining B5 containment work.
Additional evidence nuance: serialization errors inside record_report are already caught and surfaced as explicit quarantine errors. An fsync failure can leave a file whose status is stopped while the caller raises UnsafeScratch; tests should explicitly distinguish this uncommitted evidence from a successfully returned verdict. The current fsync test checks the exception/replay, not that distinction.

R3 — P2 / MEDIUM, confidence 10/10: malformed/unknown worker metadata is still accepted as a valid quiet snapshot and can produce stopped.
Evidence: driver :363-370 requires only a nonempty lifecycle string and a dict createCommand. Native modes/daemon/daemon-worker-protocol.ts :18 defines lifecycle as starting|ready|recovering|stopping|failed; :58-62 requires createCommand.type="create". The checked-in purported valid stale fixture at tests/test_prime_agent_scratch_smoke.py :693-699 instead uses lifecycle="stopped" and createCommand={}; another fixture uses "running". My full-smoke fake placed lifecycle="unknown", createCommand={} in a filesystem descriptor and used FakeOSNativeRunner for both postflights, including absent captured supervisor/worker PIDs. Both observations were valid and the persisted report said stopped. A positive control using lifecycle="ready", createCommand={"type":"create"} also stopped. Both cases preserved redaction and refused same-root replay.
Invariant/impact: contract requires malformed/unknown descriptors to remain unresolved. An absent PID is not permission to bless unsupported metadata. The returned stopped claim therefore lacks the promised complete valid evidence; this experiment did not demonstrate a surviving process.
Root cause: native shape validation remains a loose field-presence test, and the fake acceptance fixtures encode impossible native values, masking the gap.
Repair direction: validate and normalize supported native descriptor versions and relevant discriminants/nested routing fields before stale metadata can count as valid evidence. Use source-derived positive fixtures, including real lifecycle and durable create-command shapes.
Constraints/avoid: do not accept arbitrary nonempty strings, silently drop invalid fields, use native discovery's permissive filter as proof of validity, or log authenticationToken/create-command contents. Preserve the stricter required process identity and scope checks.
Tests: positive supported v1/v2 descriptors with native enum/discriminant values and absent identities can be stale; negative unknown lifecycle, absent/wrong create type, malformed nested fields and unsupported versions must prevent stopped. Inject descriptor read/stat/JSON failures and errors after partial scans. Same-root replay must add zero starts/stops for positive and rejected cases.
Regression/dependencies: repair with R1's distinction between supervisor and worker artifacts. Avoid rejecting valid version-specific native records or treating malformed records as absent; retain B1's worker veto before shutdown.

R4 — P2 / MEDIUM, confidence 10/10: native command capture can lose its child handle on setup failure, and forced reaping is not bounded.
Evidence: NativeRunner.command launches Popen at driver :482, then constructs DefaultSelector at :487, outside the cleanup try beginning :491. A fake Popen plus an injected selector-construction OSError returned with the fake child still un-killed and its pipes unclosed. Separately, timeout/overflow and unexpected pump cleanup call proc.wait() with no timeout at :519 and :526. My timeout fake recorded wait(timeout≈1) followed by wait(timeout=None). The fake returned immediately; no hung real process was created.
Invariant/impact: the command and failure path must remain bounded and exception-safe. A resource/setup failure after spawn can leave the direct start/shutdown CLI running after the driver reports uncertainty. An unbounded reap can prevent postflight and report forever. This is separate from the acknowledged possibility of detached native descendants after an uncertain start.
Root cause: ownership of the spawned direct child does not immediately enter a protected lifecycle, and the deadline excludes termination/reaping.
Repair direction: put all post-spawn setup under child-lifecycle containment and apply a finite total cleanup/reaping budget. If direct-child termination cannot be confirmed within it, retain explicit unresolved child/outcome evidence and continue only permitted read-only/reporting work. This can be proved with injected handles.
Constraints/avoid: only the exact spawned handle may be terminated; no arbitrary PID/group signals, global shutdown, mutation retry, or root cleanup. Do not assume kill implies confirmed exit or promote a timed-out shutdown because later scans are empty.
Tests: positive successful/capped capture and exact-child termination. Negative timeout/overflow remain unresolved. Inject selector construction/register/select/read failures, kill failure, and wait refusal to finish; assert finite timeout arguments, retained evidence, redaction, no extra native mutation and safe handling of already-exited children. Replay retained interrupted stages with zero new starts/stops.
Regression/dependencies: pair with R2. Do not sacrifice concurrent stdout/stderr draining or useful bounded digest/count evidence; a failed termination must not be reported as a completed shutdown.

B4 RESIDUAL AND SCOPE
The documented macOS Python 3.9 check-to-exec gap is not a separate blocker against the prior B4 remedy: that remedy required physical identity and revalidation before native use, not an atomic executable-descriptor launch. Current checks substantially implement that bounded guarantee and explicitly disclaim atomic binding. This does not authorize concurrent executable retargeting or a stronger adversarial same-uid claim. Helper cwd is now recorded separately; native daemon-command.ts :701-705 uses the supplied scratch --cwd for the detached daemon. I infer no new product/scope decision or native-run authority.

Repair R1/R3 together and R2/R4 together, reconcile candidate evidence, then obtain a fresh exact-pushed-commit review. Wrapper-only PASS remains separate. The faux-provider/tool-root slice and blocked Phase 3a EPISODE remain untouched and outside this review. No core change or scope expansion is required to act on these findings.
