# Episode cwd divergence — fork versus recovery (2026-09-26)

**Verdict: fork causation UNKNOWN; execution-root harm NOT demonstrated.** This read-only review corrects an earlier time-zone error and records an isolated test plan. It does not authorize a live switch, handoff, or a change to episode creation.

## Exact observed sequence

- Initial creation code calls `SessionManager.forkFrom(sourceSessionFile, worktree)` (`spec-episode.ts:575–577`), then daemon `create(lifecycle:resident, config.cwd=worktree)` and checks the returned cwd (`:766–802`). The owned JSONL header created 2026-09-25 19:17 UTC records the worktree cwd.
- An in-episode `ipython` call returned `os.getcwd()` as the worktree at 2026-09-26 01:27 UTC. No further root-session tool call appears after that result in the owned JSONL.
- Read-only supervisor logs show old workers receiving SIGTERM at 01:33 UTC, restart/adoption activity, and a replacement worker listening at 01:35:07 UTC. The owned session appended `session_state=active` at 01:35:07.503 UTC. The current public `list(all:true)` and `get_state` match the exact durable ID/file and report the builder cwd. `list` maps the current route to a worker whose OS cwd, and whose child `python -m rlm.repl` process's **idle** OS cwd, are also the builder cwd.
- `ps lstart` prints local PDT, while JSONL and logs use UTC. The current worker/kernel started **after** the 01:27 worktree result. They cannot be assumed to have produced that earlier result. The current public route differs from the route retained in the original identity record; route refresh is expected to be mutable, not a new durable identity.

The sequence places the observed mismatch after a daemon restart/rebind, not at initial fork creation. The logs do not prove why the recovered session's public cwd is the builder cwd, whether fork ancestry mattered to recovery, or where the next built-in or Python tool would execute. Idle process cwd is not execution-time tool cwd. Do not claim that removing a plugin check is safe **or** harmful solely from these metadata snapshots.

## Operator hypothesis and controlled next test

Joe noted that a full conversation fork is compacted soon afterward and the episode relies on plan/spec/Beads/`/prepare`; a fresh session could avoid inherited context and possibly cwd lineage. `VISION.md:190–208` explicitly treats how much context crosses as an empirical tradeoff, while the archived worktree-isolated-episodes spec `:266–285` required a full fork and the later oversight spec `:346–365` compacts inherited planning context. Personal-life brain keyword and hybrid queries had no relevant compiled fork-decision page; those repo documents are the source for this rationale.

A bounded **scratch-only A/B** should create one source in builder, then A with `SessionManager.forkFrom(source, worktree)` and B with a fresh session rooted in worktree. Publish both via isolated daemon `create(config.cwd=worktree)`, record exact ID/file/route, header, `list`/`get_state`, perform a controlled restart of only scratch-owned processes, and compare recovered metadata. If a safe isolated tool path is available, measure execution-time `ipython os.getcwd()` and bash/relative resolution before and after restart for each. Such a probe writes scratch transcript/kernel/session state and was **not** run in this read-only review. A-only failure would support fork-specific recovery; both failing would support generic recovery; metadata drift with worktree execution would not establish tool-root harm. Preserve strict scratch ownership, cleanup, and no live process contact.

No plugin source, user-global copy, live episode route, session file, or terminal state was changed. The active Slice 1 remains blocked; the operator rules out Prime Agent core updates or unsolicited PRs. This report supplements the prior synthetic-switch evidence at commit `5a8fdd5cfbbdb67be35229dbdf860760d85b7bd2` and was reviewed by an ordinary read-only investigator (`sub-d1ebdb1e`, `openai-codex/gpt-6-sol`), not a required final EXPERT.

## Scratch A/B preflight gate — BLOCK (no run)

A separate ordinary scratch-fixture worker (`sub-352792ef`) drafted `/tmp/prime-claw-fork-fresh-ab-5mvdpw79/fixture.py` (SHA-256 `3d02eaf05048644f8abf890db9b41590b22974b3b1270e1f5172da80ebf13252`). The owner independently syntax-checked it but did **not** execute it, restart a process, dispatch a provider, or modify the live episode.

The owner blocks this draft before any mutating probe:

1. Its `ROOT = Path(__file__).resolve().parent` resolves macOS `/tmp` to `/private/tmp`, but preflight requires literal `ROOT.parent == Path('/tmp')`. That guard fails on this host even for the intended scratch directory. Canonicalize the temp-root comparison without weakening scratch ownership.
2. It does not verify and stop the scratch supervisor on every exit, including uncertain create/recovery. The required wrapper deletes its isolated config/session stores on exit, potentially erasing descriptors while leaving a detached scratch process. Require owner-verified supervisor/worker teardown and preserve enough scratch evidence to resolve uncertainty before wrapper cleanup. Never use a global `prime-agent shutdown` or broad process kill.
3. Its restart uses `SIGKILL` on a verified worker. The observed incident involved workers receiving `SIGTERM` during supervisor replacement; a worker-crash-only test cannot establish the cause of that later rebind. Design a bounded scratch-only restart matching the observed path, or label a worker-crash test as narrower. Exact descriptor, PID, argv, socket, and lifecycle checks must fail closed before any signal; no blind retry after uncertain mutation.
4. The draft checks metadata only. It cannot establish the next tool's execution root. The installed custom-provider docs describe a deterministic local `streamSimple` route for a later isolated `ipython`/bash/sentinel test, but that route is unverified. `--offline` alone does not make inference local.

Next: revise the scratch harness, review the exact revised preflight and teardown, then decide whether a bounded isolated run is safe. No fork-to-fresh product change, plugin guard removal, or live recovery follows from this blocked draft.

## Revised scratch gate — still BLOCK (no launch)

The same worker revised only the temporary script to SHA-256 `cbb5ee01dba6424094352f308b5e01751480903ba2dae147c2f4dd9c434134d8`. Parent review confirmed the canonical `/tmp` check now accepts macOS `/private/tmp` with current-user 0700 scratch HOME/TMPDIR checks. Syntax check passed. Its `main()` has an unconditional `PRE-RUN BLOCK` exception after local preflight and **before** `seed`, socket creation, daemon `create`, or a signal. No native probe ran.

The remaining block is architectural: no proven bounded teardown of every scratch supervisor/worker across successful and uncertain create/recovery before `scripts/run-prime-agent-probe.sh` deletes its config/session stores. Installed `prime-agent shutdown` help says it stops every agent/background service at its target; the owner has not proved that a socket override confines it to the scratch daemon. Direct worker/supervisor signals can leave replacements, and a timeout may hide the new route. The draft's remaining worker-crash control is explicitly narrow, not an incident-matching supervisor replacement or fork-causation result. A future run requires evidence for exact scoped descriptor/PID/argv/socket ownership, bounded teardown under uncertain outcomes, and zero scratch processes before wrapper exit. Read-only research into supported scoped shutdown or safer SDK-only comparisons may narrow this block; neither substitutes for live execution-root proof or authorizes a product change.

## Read-only alternative-path review — detached A/B still BLOCK

Two ordinary read-only workers (`sub-8acc9a3f` and `sub-096aea24`) inspected the installed 0.9.6 release and project tests; neither ran a probe or edited source. `CHANGELOG.md:387–388` says daemon discovery is scoped to the isolated HOME/agent-dir state root, so `shutdown --force` no longer reaches another root. `docs/daemon.md:29–40` and `README.md:530–535` say it stops the targeted supervisor and workers. But `--daemon-socket` is documented only as canonicalizing descriptor namespaces (`CHANGELOG.md:605–610`), and no installed public descriptor schema, exact PID/argv/socket ownership algorithm, or complete teardown/uncertain-outcome contract was found. Historical `daemon shutdown --all` is machine-wide (`CHANGELOG.md:1049–1052`) and forbidden here. The wrapper's unconditional EXIT cleanup is not a shutdown gate. State-root scoping is useful evidence, **not** proof that the proposed fixture safely cleans every route and supervisor after failure.

`docs/session-format.md:402–406,451–454` documents `SessionManager.create`, `forkFrom`, `open`, and cwd/file/ID inspection; `docs/sdk.md` describes cwd-bound built-in tools. Yet this installed compiled release has no importable `dist/index.js` or declaration file. The only standalone SDK found was an older 0.80.6 generation and is not a valid 0.9.6 comparator. A temporary extension loaded by the exact native binary could inspect scratch manager metadata, but still needs safe daemon-process cleanup; a standalone metadata reconstruction would not prove the live supervisor rebind or execution-time kernel/built-in cwd. No supported direct tool execution route was validated. The detached integration run therefore remains blocked while a bounded **read-only** exact-version source lookup is considered. No Prime Agent core update or PR, live action, plugin guard removal, or fork-to-fresh design change is authorized by this review.
