{
  "spike": "phase1-credentials",
  "captured_at": "2026-09-11T17:30:38.037422+00:00",
  "requirement": "R-U1-6 (GATE track: AI Gateway)",
  "status": "RESOLVED \u2014 GATE PASSES",
  "correction": "The earlier 'istio RBAC rejects OpenShell proxy' blocker conclusion was WRONG \u2014 it was Joe's GlobalProtect VPN disconnect/reconnect (confirmed by Joe). The 403 was transient.",
  "proof": {
    "mechanism": "Custom profile zd-ai-gateway (endpoint ai-gateway.zende.sk, x-api-key) + provider prime-claw-ai-gateway (shared gateway key). Policy model_ai_gateway allows ai-gateway.zende.sk:443 rest to curl+node.",
    "placeholder": "api_key env holds openshell:resolve:env:v1371046159418530048_api_key (opaque placeholder).",
    "l7_rewrite_works": "Supervisor log ALLOWED POST .../anthropic/v1/messages [engine:l7]; placeholder rewritten to real key at L7.",
    "GET /anthropic/v1/models via proxy": "200 OK, full model list returned (proves placeholder->real key substitution).",
    "GATE \u2014 POST /anthropic/v1/messages model=anthropic.kimi-k3 via proxy": "200 OK, assistant reply 'PROOF_OK', model accounts/fireworks/models/kimi-k3, usage tokens present. REAL model call through the in-sandbox proxy with placeholders-only in env.",
    "istio RBC actual config": "authorizationpolicy/ai-gateway-nacl on istio-gateways: ALLOW from remoteIpBlocks [34.59.157.104/32,98.90.160.8/32,34.135.52.162/32] to hosts [ai-gateway.zende.sk,...] on istio-public-ingressgateway. A plain docker container from the same Mac also succeeds -> source-IP is NOT the gate; the path istio sees is fine.",
    "consumption_path_note": "prime-agent auth precedence = runtime > stored auth.json > env. So in-sandbox must run with NO stored anthropic cred in auth.json so it falls through to env; env bridge needed because OpenShell injects generic 'api_key' but prime-agent reads ANTHROPIC_API_KEY. baseUrl from models.json (config copy) targets the gateway."
  }
}
