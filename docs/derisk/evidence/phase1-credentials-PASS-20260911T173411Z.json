{
  "spike": "phase1-credentials",
  "captured_at": "2026-09-11T17:34:11.117917+00:00",
  "requirement": "R-U1-6",
  "status": "ALL SUB-CONDITIONS PASS (GATE track)",
  "target": "AI Gateway (D12/D13): ai-gateway.zende.sk/anthropic, shared key, default model anthropic.kimi-k3",
  "setup": {
    "profile": "zd-ai-gateway (custom): endpoint ai-gateway.zende.sk:443 rest, credential api_key, auth_style header x-api-key, no binary gate",
    "provider": "prime-claw-ai-gateway carrying the shared gateway key (read from host auth.json at apply time; never committed)",
    "policy": "model_ai_gateway: ai-gateway.zende.sk:443 rest read-write, binaries curl+node",
    "consumption_path": "prime-agent auth precedence = runtime > stored auth.json > env. Sandbox has NO auth.json (so env is used). baseUrl carried via models.json (authorized config copy). Env bridge: OpenShell injects generic 'api_key'; prime-agent reads ANTHROPIC_API_KEY -> export ANTHROPIC_API_KEY=$api_key."
  },
  "results": {
    "(a) placeholders-only env": "PASS \u2014 env shows api_key=<openshell:resolve:env:...>; no zdai_ material",
    "(b) real model call": "PASS \u2014 `prime-agent -p --no-tools` with ANTHROPIC_API_KEY=$api_key (placeholder) returned 'AGENT_OK'; direct curl POST anthropic.kimi-k3 returned 'PROOF_OK' (200, model accounts/fireworks/models/kimi-k3, token usage present)",
    "(c) no creds on disk": "PASS \u2014 grep -rIlE 'zdai_' /sandbox returned no files",
    "(d) fail-closed": "PASS \u2014 bogus placeholder openshell:resolve:env:nonexistent_key -> HTTP 500 (credential_unavailable), request not forwarded with a real key"
  },
  "note": "Earlier 'istio RBAC structural blocker' conclusion was WRONG: it was a GlobalProtect VPN disconnect. Confirmed via kubectl that ai-gateway-nacl ALLOWs the path and a plain docker container from the same Mac succeeds."
}
