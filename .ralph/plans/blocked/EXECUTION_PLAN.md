# Execution Plan — Finish the Phase 3a brain-hosting tracer bullet

> **Status:** Operator-approved active EPISODE; Slice 1 paused at an external authorized-egress blocker (2026-09-25). No later slice has begun.
> **Approved specification:** [SPECIFICATION.md](SPECIFICATION.md) in this active episode bundle.
> **Tracking:** `prime-claw-zwg`; source/Qwen prerequisite `prime-claw-zwg.5`; routed write `prime-claw-zwg.4`.

## Result and starting point

Deliver one sandboxed, cited brain read and one routed, indexed, remote-pushed brain write on Joe's explicitly selected local-Qwen/4096 profile, while retaining portable gateway/1536 behavior and a reversible old database. Preserve the already working sandbox, clone/index/query, portable defaults, repository selection, candidate-build watchdog, and credential isolation. Do not reimplement completed Slices 0–3, 4P, or 4R.

`bin/prime-claw` already exposes `embedding-preflight`, `embedding-build`, `validate`, `create`, and `converge`. Candidate build is parallel, guarded, and intentionally does not cut over. Selected-profile validation has a fail-before-mutation state guard but currently runs probe writes; plan its final behavior accordingly. The tracked Phase 3a inventory marks R3a-14 pending and does not yet contain final write/acceptance entries. Existing `stage_brain_clone` can print `ff skipped` on a divergent local checkout; `stage_brain_index` still calls `gbrain sync --skip-failed`. Neither behavior may be mistaken for the approved fail-closed source contract. The plan repairs both before trusting resumed indexing. The historical `.ralph/plans/archive/phase3a-bufd-superseded-incomplete/` bundle is provenance, not an active execution plan; its older HTTP 502 and 952-page checkpoints are stale. `docs/derisk/3a-slice4a.md` and `LONG_RANGE_PLAN.md` also contain older point-in-time checkpoints. Use the latest Bead `prime-claw-zwg.5` and fresh sanitized read-only evidence before any live action; never infer provider, Git, or database health from a document.

As last verified, canonical `gbrain` had 1,059 pages / 3,031 valid 1536d chunks and its prior bookmark; isolated `gbrain_qwen4096` had 1,057 pages / 3,029 valid 4096d chunks, no bookmark, and no cutover. The validated two-file source repair is commit `5dde001` local to `/sandbox/brain` only. Sandbox GitHub fetch/pull failed with a connection reset, so remote publication and another build are blocked. Source HEAD and expected counts must be re-established after Git reconciliation; these figures are provenance, not immutable acceptance targets.

## Shared iteration contract

Each slice below is one bounded EPISODE iteration: implement only that increment, run focused offline tests (`tests/test_brain_repo_config.py`, `tests/test_runtime_converge.py`, `tests/test_brain_query.py`, `tests/test_embedding_preflight.py`, `tests/test_embedding_candidate_build.py`, `tests/test_portable_provider_defaults.py`, `tests/test_runtime_validate.py`, and any new slice tests) plus maintained `python3 -m pytest -q tests`, `python3 -m py_compile bin/prime-claw`, and `git diff --check`; update current docs and the relevant Bead, commit and push the prime-claw slice for exact owner review. Where the capability is a live operational change with no code delta, record sanitized evidence and a current-doc/Bead update and push that evidence commit instead of inventing code. The owner verifies the exact pushed candidate and obtains independent EXPERT review when required by the active oversight procedure before advancing. Do not merge, abandon, clean episode resources, or expand scope without the appropriate operator decision.

A failing external prerequisite stops its slice safely, preserves both databases and all source evidence, records the blocker in the Bead/active execution record, and follows the existing execute blocked-plan procedure. No automatic retry, `--skip-failed`, force-push, credential workaround, or apparent partial-success receipt. A long sync gets one owned, bounded non-steering watch and cancellation on stop/completion; it must not launch a duplicate build. Operator confirmation is needed where the external service or host transport cannot be repaired internally.

## Slice 1 — Safe brain-source publication

**Working capability:** the existing in-sandbox brain clone can fetch/reconcile and push validated source through its configured private Git remote and OpenShell L7 path; the two repaired frontmatter files are present at the verified remote commit. This removes the actual Git blocker before embedding work.

- Diagnose the failing authorized sandbox Git transport and repair only its proper host/OpenShell/VPN/egress seam if within approved authority. Do not use DGX to push source, copy credentials into the sandbox, or work around the provider boundary. Recheck the private remote branch before publishing; reconcile remote advancement without force and rerun both source frontmatter validations. Treat the absent third path through supported full-sync reconciliation, not manual failure-ledger edits. Replace `ff skipped` success with truthful divergence handling in the clone path: local commits must not be silently stranded or discarded; no force or implicit merge of unreviewed remote changes.
- Verify remote contains the repaired commit (or a safely reconciled descendant), that the sandbox clone is clean and push-capable, and that no gbrain sync/build is active. Recheck canonical/candidate identity without mutating either DB. If transport remains externally blocked, stop here; do not start Slice 2.
- Add focused offline tests only if repository-controlled transport behavior changes. Record a sanitized transport/source receipt and update `docs/derisk/3a-slice4a.md` plus `prime-claw-zwg.5`. Commit/push the prime-claw checkpoint for exact owner review. Do not claim this slice complete merely because the repair is still local.

### Slice 1 checkpoint (2026-09-25) — partial safety fix; remote publication blocked

A read-only sandbox probe found local brain commit `5dde0012eadf1df7c3e9c83d228e2d0d31f36695` on clean `main`, with old local tracking ref `5a477eaa5b311b9b45324591dd308e83ddcd1ded`; this is not a verified remote HEAD. A bounded in-sandbox `git ls-remote` failed with `connection_reset`; both GitHub HTTPS and allowed `example.com` canary failed HTTP 000 / curl 56. Host GitHub HTTPS returned 200, but `zetup vpn status` returned `not-connected`. No credentials or databases were inspected/changed, and no Qwen call, source push, or build occurred. Evidence: `docs/evidence/phase3a-slice1-transport-20260925.json` (path relative to repository root).

An offline code/test increment makes the clone stage fail closed on fetch failure (`pipefail`) and local non-fast-forward divergence (one attempt, no `ff skipped` success); this is not a complete Slice 1. **Unblock condition:** Joe restored authorized VPN, but the bounded post-reconnect sandbox Git probe still reset and even allowed `example.com` stayed at HTTP 000 / curl 56. OpenShell reports gateway healthy, policy effective, and service running. The owner/operator must coordinate safe host/OpenShell sandbox-egress repair that preserves both databases and the credential boundary; no sandbox restart, provider update, or policy change was attempted. Then take one fresh sandbox egress/Git check, read actual remote HEAD, reconcile without force, revalidate both repaired frontmatter files, push, and verify exact remote receipt. Do not mistake VPN reconnection or a local tracking ref for a push receipt. Slice 2 and Qwen work remain forbidden until Slice 1 is accepted.

## Slice 2 — Complete an isolated 4096d source-current candidate

**Working capability:** the parallel candidate is a complete, source-current 4096d brain index and can answer known exact-page and semantic questions, while the runtime still uses its unchanged canonical 1536d configuration. No cutover in this slice.

- Require fresh operator clearance if the private embedding service is not known healthy. After Slice 1's remote-source receipt, take bounded exact-model host and in-sandbox 200/4096 preflights and verify policy restoration, zero pre-existing sync processes, intact canonical snapshot, candidate identity, and source HEAD. If the bounded host probe fails, do not run the sandbox probe or build. One isolated build may resume the preserved candidate only after those gates; its durable-progress watchdog retains the existing process-group stop and false-success protections.
- Before any new indexing, remove the existing ordinary `stage_brain_index` second-pass `gbrain sync --skip-failed` and its misleading nonzero masking; fail closed on malformed sources and verify a true full-source bookmark. Require a successful full-source bookmark at the reconciled source HEAD, complete source/page/chunk coverage, all current-text 4096d vectors with zero null/mixed/stale rows, supported exact and semantic retrieval, and evidence that corporate gateway embeddings were not used. Compare with the legacy database only where the source revision makes counts comparable; do not accept keyword-only retrieval. On source/provider failure, preserve candidate and canonical state and record the exact stop; never skip files to manufacture success.
- Extend `tests/test_embedding_candidate_build.py`, `tests/test_embedding_preflight.py`, `tests/test_runtime_converge.py`, and focused validation tests for no-skip failure handling, full-source parity/freshness, failure restoration, retrieval, and sanitized proof. Update `docs/derisk/3a-slice4a.md`, sanitized evidence, and `prime-claw-zwg.5`; run shared gates and push an exact reviewed checkpoint.

## Slice 3 — Accept and safely switch Joe's selected profile

**Working capability:** Joe's explicitly selected sandbox runtime uses the accepted local-Qwen/4096 database for queries and embeddings; an independently verified switch back to the intact gateway/1536 database remains possible. Operators without a local override still use tracked gateway defaults.

- Gate the switch on Slice 2's exact source/embedding/retrieval/non-use evidence. Preserve a verified old selection before switching. Atomically select the candidate and its narrowly scoped egress policy as a complete runtime outcome: if either configuration, policy, or post-switch checks fail, restore and verify the old selection or block without claiming success. Ensure `create`/`converge`/`validate` resolve the actual selected profile rather than silently indexing or testing the old one. Test repeated cutover and rollback as reversible configuration selection, not an in-place vector rewrite or database drop. Do not rely on status-provider compatibility bugs as evidence of a healthy/failed brain.
- Add focused offline cutover/rollback, profile-precedence, policy restoration, fail-closed config, and validation tests. Verify the live selected state, rollback readiness, no old DB mutation, and portable defaults with sanitized evidence. Update `docs/derisk/3a-slice4a.md`, relevant runbook/current configuration docs, requirement status, and `prime-claw-zwg.5`; run shared gates and push the reviewed slice. Only owner acceptance of this slice unlocks Joe's Slice 4.

## Slice 4 — One routed brain write reaches the remote

**Working capability:** the sandboxed Prime Agent writes the operator-approved keep-worthy `projects/prime-claw` page via gbrain, indexes and retrieves it on the selected profile, commits and pushes it from the configured sandbox clone, and can show a remote-confirmed receipt. Bead `prime-claw-zwg.4` remains blocked on accepted Slice 3 by Joe's ordering, not a portable-platform dependency.

- Follow the store-selection boundary in `docs/information-architecture.md` (domain fact to operator brain; no tracked prime-claw taxonomy), recall before writing to avoid duplicate or destructive content, and propose the page body for Joe's approval before the live personal-brain mutation. The previously approved slug alone is not approval of arbitrary content. Use the existing `gbrain put` / `gbrain sync --source brain` path and L7 Git provider. Handle non-fast-forward and partial push failures without losing the local commit or falsely claiming success. The receipt includes slug, indexed read/citation, exact commit, verified remote result, and credential isolation, with no secret or private endpoint.
- Add offline tests for routing/put/sync/commit/push ordering, duplicate/retry safety, remote failure receipts, and no sandbox credential material. Preserve the generic gateway path. Record sanitized live proof, update current write/runbook docs and `prime-claw-zwg.4`, run shared gates, and push the prime-claw slice for exact review.

## Slice 5 — Repeatable whole-tracer-bullet acceptance

**Working capability:** a supported `bin/prime-claw validate` or dedicated acceptance command reports truthful pass/fail for the configured brain source and `.git`, fresh selected vector space, cited query, and remotely confirmed write, while existing runtime/gateway checks remain intact.

- Reconcile live acceptance with the selected Qwen cutover and write receipt. Validation must not claim a partial/keyword-only index, local-only commit, stale bookmark, failed profile switch, or a probe write as the routed-write proof. Keep validation's own write probes clearly distinct from the approved `projects/prime-claw` write and fail before any mutating probe when selected state is invalid. A fresh `create`/`converge` remains idempotent; prove via safe bounded live checks or hermetic dry-run rather than destructive recreation.
- Extend focused validation and inventory-integrity tests. Reconcile `config/requirements-inventory.json` R3a-0..16 with actual proof and correct the stale Phase 2 R2-A-3/R2-A-4 statuses only from existing accepted evidence. Update `LONG_RANGE_PLAN.md`, `docs/derisk/3a-slice4a.md`, runbook/current docs, sanitized final evidence, and Beads without rewriting old evidence. Run focused and full maintained suites, link validation receipts, and push the final candidate for exact owner/EXPERT review. Close Phase 3a only after the owner verifies acceptance and the operator chooses terminal disposition.

## Dependency and non-goal boundaries

Slice 1 precedes a resumed candidate build; Slice 2 precedes the cutover; Joe requires accepted Slice 3 before Slice 4; Slice 5 requires the routed-write receipt. A healthy embedding provider or restored Git transport cannot be assumed from prior snapshots. If repair needs host permission, interactive authentication, or external service recovery, stop and request the exact operator action rather than silently bypassing the boundary. Keep other episodes and the canonical checkout intact.

No new brain taxonomy, public/example brain repo, credentials on sandbox disk, private endpoint in tracked material, in-place database migration, premature cutover, corporate-gateway embedding route for local Qwen, `--skip-failed`, automatic model/service retry, unrelated provider-status fix (`prime-claw-zwg.3`), full memory skill port, browse/channel/schedule work, new orchestrator, or new general lifecycle machinery. This plan does not approve its own execution; only the separate `/implement-spec .ralph/plans/future/phase3a-brain-hosting-completion` gate can create an episode after plan review.
